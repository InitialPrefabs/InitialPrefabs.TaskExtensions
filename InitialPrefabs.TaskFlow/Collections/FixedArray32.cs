using System.Runtime.CompilerServices;

namespace InitialPrefabs.TaskFlow.Collections {

    public unsafe struct FixedArray32<T> where T : unmanaged {
        // TODO: This needs to store the Handle so we can have a dependency chain
    }
}

